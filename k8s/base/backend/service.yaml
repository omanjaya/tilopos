apiVersion: v1
kind: Service
metadata:
  name: tilopos-backend
  labels:
    app: tilopos-backend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: tilopos-backend
